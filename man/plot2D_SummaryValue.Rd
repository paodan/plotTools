% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot2D_SummaryValue.R
\name{plot2D_SummaryValue}
\alias{plot2D_SummaryValue}
\title{Plot 2D Summary Values in Hexagonal Bins}
\usage{
plot2D_SummaryValue(
  data,
  x,
  y,
  z,
  summaryFun = mean,
  bins = 100,
  low = "grey",
  mid = "white",
  high = "red",
  midpoint = 0,
  legendName = z,
  title = z
)
}
\arguments{
\item{data}{A `data.frame` containing the variables to be plotted.}

\item{x}{A string giving the name of the column in `data` to be used as the x-axis.}

\item{y}{A string giving the name of the column in `data` to be used as the y-axis.}

\item{z}{A string giving the name of the column in `data` to be summarized within bins.}

\item{summaryFun}{A function (default = `mean`). The summary function applied to `z`
values inside each hexagonal bin. Common choices include `mean`, `median`, `sum`, etc.}

\item{bins}{Integer (default = 100). The number of bins along each axis
for hexagonal binning.}

\item{low}{Color for low values (default = `"grey"`).}

\item{mid}{Color for midpoint values (default = `"white"`).}

\item{high}{Color for high values (default = `"red"`).}

\item{midpoint}{Numeric (default = 0). The midpoint for the diverging color scale.}

\item{title}{Character string giving the plot title (default = the name of `z`).}
}
\value{
A `ggplot` object showing hexagons colored by the summarized values of `z`.
}
\description{
This function visualizes the summarized values of a third variable (`z`)
across a 2D space defined by variables (`x`, `y`) using hexagonal binning.
Each hexagon shows the summary statistic of `z` for all points inside that bin.
}
\details{
The function uses `ggplot2::stat_summary_hex` to compute summary statistics
of the variable `z` in each hexagonal bin defined by (`x`, `y`).
The color of each hexagon reflects the summarized value of `z`, with colors
interpolated between `low`, `mid`, and `high` around the specified `midpoint`.

The function applies a custom theme (`theme_Publication()`) for aesthetics.
Make sure this theme function is available in your environment
(otherwise replace with `theme_classic()` or other `ggplot2` themes).
}
\examples{
\dontrun{
  library(ggplot2)

  set.seed(123)
  df <- data.frame(
    x = rnorm(5000),
    y = rnorm(5000),
    z = rnorm(5000)
  )
  p <- plot2D_SummaryValue(df, x = "x", y = "y", z = "z", summaryFun = mean)
  print(p)
}

}
